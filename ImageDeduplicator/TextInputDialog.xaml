<Window x:Class="ImageDeduplicator.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageDeduplicator"
        mc:Ignorable="d"
        Title="TextInputDialog" Height="80" Width="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Name="inputTxt" />
            <CheckBox Grid.Row="1" Grid.Column="0" Content="Invert" Name="invertCheck" Click="invertCheck_Click" />
            <Button Grid.Row="1" Grid.Column="1" Content="OK" Name="okButton" Click="okButton_Click" Width="75" />
            <Button Grid.Row="1" Grid.Column="2" Content="Cancel" Name="cancelButton" Click="cancelButton_Click" Width="75" />
        </Grid>
        
    </Grid>
</Window>
